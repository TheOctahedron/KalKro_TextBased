[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "KalKro"
version = "0.1.0"
description = "KalKro-imports"

[tool.setuptools.packages.find]
where = ["src"]
include = ["KalKro*"]
